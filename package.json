{
	"name": "@chkt/onceupon",
	"description": "Logging without compromises",
	"keywords": [ "Log", "Logger" ],
	"version": "1.0.0-alpha.15",
	"license": "MIT",

	"author": {
		"name": "chkt",
		"email": "ck@chkt.one",
		"url": "chkt.one"
	},

	"repository": {
		"type" : "git",
		"url": "https://github.com/chkt/onceupon"
	},

	"engines": {
		"node" : ">=10.0"
	},

	"files": [ "dist" ],
	"main": "./dist/index.js",

	"scripts" : {
		"prepublishOnly": "yarn run test && yarn run lint && yarn outdated && yarn run build",

		"lint" : "./node_modules/tslint/bin/tslint -p tsconfig.json",
		"test" : "./node_modules/mocha/bin/mocha -r ts-node/register test/**/*.ts",
		"build" : "rm -rf ./dist/* && ./node_modules/typescript/bin/tsc"
	},

	"devDependencies": {
		"@types/mocha": "^7.0",
		"@types/node": "^13.7",
		"mocha": "^7.0",
		"nyc": "^15.0",
		"ts-node": "^8.6",
		"tslint": "^6.1",
		"typescript": "^3.7"
	},

	"dependencies": {
		"@chkt/continuity": "1.0.0-beta.3"
	}
}
